/**
 * Degree to radians conversion factor.
 */
export const RADIANS = Math.PI / 180.0;

/**
 * PhoriaJS Epsilon (minimum value upper to zero for operations).
 */
export const EPSILON = 0.000001;

/**
 * Two PI constant.
 */
export const TWOPI = Math.PI * 2;
